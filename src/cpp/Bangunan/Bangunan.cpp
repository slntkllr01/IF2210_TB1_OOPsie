#include "../../header/Bangunan/Bangunan.hpp"

Bangunan::Bangunan(int ID, string kode, string nama, map<string, int> listofResep, int price) : ID(ID), kode(kode), nama(nama), listOfResep(listofResep), price(price) {}
